<head>
    <link rel="stylesheet" href="/css/vitamins_page.css" crossorigin="anonymous">
</head>
<body ng-app="app">
<div  ng-controller="NewUser">
    <div class="main_wrapper">
        <div class="specific_page_header">
            Create Vitamin.
        </div>
    </div>
</body>

<div class="col-sm-6 col-sm-offset-3">

    <h1><span class="fa fa-sign-in"></span> Signup</h1>

    <!-- show any messages that come back with authentication -->
    <% if (message.length > 0) { %>
        <div class="alert alert-danger"><%= message %></div>
    <% } %>
    Note there is no authenitcation. Make sure the info you provide is fully formed and valid and it
    does not already exist in the database.
    <form action="/create_vitamin" method="post", name="vitamin">
        <div class="form-group">
            <label>Vitamin Name</label>
            <input type="text" class="form-control" name="name">
        </div>
        <div class="form-group">
            <label>Decscription</label>
            <input type="text" class="form-control" name="description">
        </div>
        <div class="form-group">
            <label>Price Per Unit</label>
            <input type="number" class="form-control" name="price_per_unit">
        </div>
        <div class="form-group">
            <label>Dose range</label>
            <input type="text" class="form-control" name="dose_range">
        </div>
        <div class="form-group">
            <label>Times Per Day</label>
            <input type="number" class="form-control" name="times_per_day">
        </div>
        <div class="form-group">
            <label>Unit Type</label>
            <input type="text" class="form-control" name="unit">
        </div>


        <button type="submit" class="btn btn-warning btn-lg">Create Vitamin</button>
    </form>

</div>